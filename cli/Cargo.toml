[package]
name = "secret-santa-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
env_logger = "0.10"
log = "0.4"
secret-santa-core = { path = "../core" }

[[bin]]
name = "secret-santa-cli"
path = "src/main.rs"
