[workspace]
resolver = "2"

members = [
    "cli",
    "core",
    "utils",
    "wasm",
]

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
